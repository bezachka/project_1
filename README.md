# Анализатор дефектов деталей вала

Приложение на Python для автоматического определения дефектов деталей вала по фотографиям с использованием методов компьютерного зрения и машинного обучения.

## Возможности

- 🔍 Автоматическое обнаружение дефектов на изображениях деталей вала
- 🎯 Классификация деталей на исправные и дефектные
- 📊 Извлечение признаков с помощью компьютерного зрения
- 🤖 Использование машинного обучения для классификации
- 📈 Визуализация результатов анализа
- 🎓 Обучение модели на собственных данных

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. (Опционально) Для использования GPU с TensorFlow установите CUDA и cuDNN

## Структура проекта

```
otsp/
├── main.py                 # Основное приложение (CLI)
├── gui_app.py              # Графический интерфейс (GUI)
├── image_processor.py      # Модуль обработки изображений
├── defect_classifier.py    # Модуль машинного обучения
├── example_usage.py        # Примеры использования
├── requirements.txt        # Зависимости
├── README.md              # Документация
└── QUICKSTART.md          # Быстрый старт
```

## Использование

### Графический интерфейс (GUI)

Для запуска графического приложения:

```bash
python gui_app.py
```

Графический интерфейс предоставляет:
- 📁 Загрузку и анализ изображений
- 🎓 Обучение модели через удобный интерфейс
- 📊 Визуализацию результатов и дефектов
- 📂 Пакетную обработку директорий
- 💾 Сохранение результатов анализа

### Командная строка (CLI)

### 1. Обучение модели

Перед использованием необходимо обучить модель на размеченных данных.

#### Вариант A: Структура директорий
Создайте следующую структуру:
```
data/
├── good/          # Изображения исправных деталей
│   ├── image1.jpg
│   └── image2.jpg
└── defect/        # Изображения дефектных деталей
    ├── image3.jpg
    └── image4.jpg
```

Затем обучите модель:
```bash
python main.py train --input data/
```

#### Вариант B: CSV файл с метками
Создайте файл `labels.csv`:
```csv
image_path,label
data/image1.jpg,0
data/image2.jpg,0
data/image3.jpg,1
data/image4.jpg,1
```
(0 = исправна, 1 = дефектна)

Затем обучите модель:
```bash
python main.py train --input data/ --labels labels.csv
```

После обучения будут созданы файлы `model.pkl` и `scaler.pkl`.

### 2. Анализ изображений

#### Анализ одного изображения:
```bash
python main.py analyze --input path/to/image.jpg
```

#### Анализ всех изображений в директории:
```bash
python main.py analyze --input path/to/images/
```

#### С указанием модели:
```bash
python main.py analyze --input image.jpg --model model.pkl --scaler scaler.pkl
```

#### Сохранение результатов:
```bash
python main.py analyze --input images/ --output results/
```

#### Без визуализации:
```bash
python main.py analyze --input image.jpg --no-visualize
```

## Примеры использования

### Полный цикл работы:

```bash
# 1. Обучение модели
python main.py train --input training_data/

# 2. Анализ нового изображения
python main.py analyze --input test_image.jpg --output results/

# 3. Анализ пакета изображений
python main.py analyze --input test_images/ --output results/
```

## Методы компьютерного зрения

Приложение использует следующие методы:

1. **Предобработка изображений:**
   - Конвертация в grayscale
   - Улучшение контраста (CLAHE)
   - Размытие для уменьшения шума

2. **Обнаружение дефектов:**
   - Адаптивная бинаризация
   - Морфологические операции
   - Поиск контуров

3. **Извлечение признаков:**
   - Геометрические признаки (площадь, периметр, округлость)
   - Текстурные признаки (GLCM: контраст, однородность, энергия)
   - Признаки интенсивности (среднее, стандартное отклонение)
   - Признаки краев (плотность краев)

## Машинное обучение

- **Алгоритм:** Random Forest или Gradient Boosting
- **Признаки:** 13 признаков, извлеченных из изображений
- **Выход:** Вероятность дефекта (0-1) и бинарная классификация

## Требования

- Python 3.8+
- OpenCV 4.8+
- scikit-learn 1.3+
- NumPy, Pandas
- (Опционально) TensorFlow для расширенных возможностей

## Примечания

- Для качественной работы модели требуется достаточно данных для обучения (рекомендуется минимум 50-100 изображений каждого класса)
- Качество анализа зависит от качества входных изображений
- Рекомендуется использовать изображения с хорошим освещением и контрастом

## Лицензия

MIT License


